// API configuration for the UB Bank Scraper frontend
// In development: Vite proxy handles /api/* -> http://localhost:3000/api/*
// In production: Same origin, no CORS needed
export const API_BASE = "/api";